# Database Configuration
# PostgreSQL connection string for Prisma
DATABASE_URL="postgresql://flexliving:password@localhost:5432/flexliving_reviews?schema=public"

# Redis Configuration
# Redis connection string for caching
REDIS_URL="redis://localhost:6379"

# Server Configuration
# Port for the Express server
PORT=4000

# Host for the Express server (use 0.0.0.0 for Docker)
HOST=0.0.0.0

# Environment (development, production, test)
NODE_ENV=development

# Frontend Configuration
# URL of the frontend application for CORS
FRONTEND_URL=http://localhost:3000

# Additional CORS origin if needed
CORS_ORIGIN=http://localhost:3001

# API Configuration
# Hostaway API credentials (replace with actual values)
HOSTAWAY_ACCOUNT_ID=61148
HOSTAWAY_API_KEY=f94377ebbbb479490bb3ec364649168dc443dda2e4830facaf5de2e74ccc9152

# Hostaway API base URL
HOSTAWAY_API_URL=https://api.hostaway.com/v1

# Security Configuration
# JWT secret for authentication (generate a secure random string)
JWT_SECRET=your_super_secret_jwt_key_change_this_in_production

# Rate limiting configuration
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# Logging Configuration
# Log level (error, warn, info, http, debug)
LOG_LEVEL=debug

# Enable request logging
ENABLE_REQUEST_LOGGING=true

# Cache Configuration
# Default cache TTL in seconds
CACHE_TTL=3600

# Cache key prefix
CACHE_PREFIX=flexliving:

# Health Check Configuration
# Health check timeout in milliseconds
HEALTH_CHECK_TIMEOUT=5000

# Webhook Configuration (for future use)
# Secret for validating incoming webhooks
WEBHOOK_SECRET=your_webhook_secret

# External Service URLs (for future integrations)
# Email service configuration
EMAIL_SERVICE_URL=
EMAIL_API_KEY=

# SMS service configuration
SMS_SERVICE_URL=
SMS_API_KEY=

# File Upload Configuration
# Maximum file upload size (in bytes)
MAX_FILE_SIZE=10485760

# Allowed file types
ALLOWED_FILE_TYPES=jpg,jpeg,png,pdf

# Docker-specific Configuration
# These are used when running with Docker Compose
POSTGRES_DB=flexliving_reviews
POSTGRES_USER=flexliving
POSTGRES_PASSWORD=password
POSTGRES_HOST=db
POSTGRES_PORT=5432

REDIS_HOST=redis
REDIS_PORT=6379
